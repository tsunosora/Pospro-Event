# Database (MySQL)
DATABASE_URL="mysql://root:YOUR_PASSWORD@localhost:3306/pospro"

# Auth
JWT_SECRET="your_long_random_secret_here"

# Server
PORT=3001

# CORS — comma-separated list of allowed frontend origins
ALLOWED_ORIGINS="http://localhost:3000"

# Notifikasi Discord
# URL webhook diatur per-toko lewat UI: Settings › Notifikasi (disimpan di StoreSettings.discordWebhookUrl).
# Notifikasi yang dikirim: order/transaksi baru, stok hampir habis, tutup shift,
# penugasan crew event, kirim Sales Order ke grup desain, dan commit GitHub.
